class IndexFundsService {
  // db = new IndexFundsDB();

  getAll = async () => {
    //const res = await this.db.getAll();
    // return res;
    return null;
  };
}

export default IndexFundsService;
